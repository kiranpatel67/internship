import 'package:flutter/material.dart';
import 'package:get/get.dart';
import 'package:internship/presentation/category_screen/state.dart';

import 'logic.dart';

class CetegoryView extends GetView<CetegoryController> {
  CetegoryView({Key? key}) : super(key: key);

  // final logic = Get.put(CategoryBinding());
  // final state = Get.find<CategoryBinding>().state;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
